О задаче
Пользователи Авито ищут услуги короткими запросами:

«автоподбор»,
«скупка телевизоров»,
«баня на дровах»,
«монтаж видеодомофонов»,
«обзвон по базе».
По каждому такому запросу в корпусе находятся тысячи объявлений исполнителей. Поиск устроен каскадом: сначала этап кандидатогенерации быстро отбирает из всего корпуса небольшое множество подходящих объявлений, а затем модели ранжирования упорядочивают только их.

Главное требование к кандидатогенерации — полнота. Если нужное объявление не попало в кандидаты, никакое ранжирование его уже не вернёт.

В этой задаче нужно решить этап кандидатогенерации: по поисковому запросу найти объявления, которые стоит передать дальше, на ранжирование.

Цель задания
Разработать решение, которое для каждого поискового запроса отбирает из корпуса до 50 объявлений — кандидатов, которые дальше уйдут на ранжирование.

Решение:

Получает на вход поисковый запрос: его текст и остальные признаки.
Находит в корпусе объявлений услуг те, что подходят под этот запрос.
Возвращает до 50 item\_id найденных объявлений.
Подходящие объявления — те, которые пользователь в итоге выбрал по этому запросу. Обычно это одно-два объявления, иногда больше. Решение должно поймать их в свои 50 кандидатов: качество — это доля подходящих объявлений, которые туда попали.

Тебе доступны три файла в формате Parquet.

1. `train.parquet` — обучающая выборка, 497 673 строки.

Каждая строка — пара «запрос — объявление»: пользователь искал по этому запросу и выбрал это объявление. В строке есть признаки запроса (колонки `search_*`) и признаки объявления (колонки `item_*`).

Пример:

`search_query: "автоподбор"`

`search_infm_params_text: "Рейтинг пользователя 4 звезды и выше"`

`item_title_raw: "Автоподбор Разовый осмотр автомобиля"`

Это значит, что пользователь искал «автоподбор» с фильтром по рейтингу и выбрал объявление «Автоподбор Разовый осмотр автомобиля».

2. `benchmark_queries.parquet` — 2 452 запроса без разметки. Для них нужно подготовить ответ.

Колонки:

- `query_id` — идентификатор запроса;
- признаки запроса в том же формате, что в `train.parquet`.

3. `benchmark_items.parquet` — корпус из 189 212 объявлений, среди которых нужно искать.

Колонки:

- `item_id` — идентификатор объявления;
- признаки объявления в том же формате, что в `train.parquet`.

Все подходящие объявления для запросов бенчмарка есть в этом корпусе.

Файлы можно скачать одним архивом по ссылке: [https://disk.yandex.ru/d/sNhfo0YOjGtufg](https://disk.yandex.ru/d/sNhfo0YOjGtufg)

### Признаки запроса

| **ПолеОписание**            |                                                                                             |
| --------------------------- | ------------------------------------------------------------------------------------------- |
| `search_query`              | текст поискового запроса                                                                    |
| `search_location_id`        | идентификатор локации, в которой выполнялся поиск                                           |
| `search_is_delivery_search` | флаг поиска с доставкой (0/1)                                                               |
| `search_infm_params_text`   | фильтры поиска в текстовом виде, например «Вид услуги Красота, здоровье»; может быть пустым |
| `search_category`           | категория, в которой выполнялся поиск                                                       |

### Признаки объявления

| **ПолеОписание**                  |                                                                                                                                                       |
| --------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `item_id`                         | идентификатор объявления                                                                                                                              |
| `item_title_raw`                  | заголовок объявления                                                                                                                                  |
| `item_description_raw`            | описание объявления                                                                                                                                   |
| `item_infm_params_text`           | параметры объявления в текстовом виде: вид услуги, тип услуги и т. п.                                                                                 |
| `item_category_id`                | категория объявления                                                                                                                                  |
| `item_microcat_id`                | подкатегория объявления: более детальное деление внутри категории                                                                                     |
| `item_price`                      | цена                                                                                                                                                  |
| `item_rating`                     | рейтинг по отзывам                                                                                                                                    |
| `item_rating_reviews_count`       | количество отзывов                                                                                                                                    |
| `item_location_id`                | идентификатор локации объявления                                                                                                                      |
| `item_latitude`, `item_longitude` | координаты объявления                                                                                                                                 |
| `item_is_phone_hidden`            | флаг: телефон скрыт                                                                                                                                   |
| `item_is_message_forbidden`       | флаг: сообщения запрещены<br>Ответ нужно подготовить в файле `answer.csv` — CSV с разделителем «запятая», в кодировке UTF-8, ровно с двумя колонками: |

| **КолонкаТипОписание** |        |                                                                                         |
| ---------------------- | ------ | --------------------------------------------------------------------------------------- |
| `query_id`             | строка | идентификатор запроса из `benchmark_queries.parquet`, ровно 16 символов (регистр важен) |
| `answer`               | строка | до 50 `item_id` через **пробел**                                                        |

`item_id` — строка ровно из 16 символов `0-9a-f` (например, `1382564bf8994a83`), ровно в том виде, в каком она лежит в `benchmark_items.parquet`.

Требования к файлу:

- строка на каждый `query_id` из `benchmark_queries.parquet`: без пропусков, лишних строк и повторов; порядок строк любой;
- не больше 50 `item_id` в одной строке, без повторов внутри строки;
- все `item_id` должны существовать в `benchmark_items.parquet`;
- нужно возвращать именно идентификаторы объявлений (`item_id`), а не заголовки или номера строк;
- других колонок, в том числе колонки с индексом, быть не должно.

Пример `answer.csv`:

```erlang
query_id,answer
00WuFMaXSFZBxSzT,1382564bf8994a83 121fa7f5e765ce00 f3801ec4fa472597 ...
03ztb1gtRFC4K4vP,b0dd913d84ca1f61 e36f6c3245e05ab6 a3c3aa4703977bfe ...
svg
                  
```

### Сохранение предсказаний в Python

Здесь `query_ids` — идентификаторы запросов из `benchmark_queries.parquet`, `predictions` — для каждого запроса список до 50 `item_id`.

```bash
answer = pd.DataFrame({
    'query_id': query_ids,                                   # строки
    'answer': [' '.join(top50) for top50 in predictions],     # строки
})
answer.to_csv('answer.csv', index=False)
svg
                  
```

### Целевая метрика

Решение будет оцениваться по метрике **Recall\@50**.

Для одного запроса считается доля подходящих объявлений, которые попали в твой топ-50. Затем значение усредняется по всем запросам:

```css
Recall@50 = mean_по_запросам( |топ-50 ∩ релевантные| / |релевантные| )
svg
                  
```

Пример на трёх запросах:

| **ЗапросПодходящих объявленийИз них в твоих 50Вклад запроса** |   |   |           |
| ------------------------------------------------------------- | - | - | --------- |
| А                                                             | 1 | 1 | 1/1 = 1.0 |
| Б                                                             | 2 | 1 | 1/2 = 0.5 |
| В                                                             | 1 | 0 | 0/1 = 0   |

Recall\@50 = (1.0 + 0.5 + 0) / 3 = 0.5.

Порядок объявлений в ответе на метрику не влияет.

У тебя будет 7 попыток. Засчитывается наилучший результат.

### Ограничения и условия

- Запрещены хардкод ответов под конкретные `query_id` и попытки вручную восстановить тестовую разметку.
- Запрещены плагиат и копирование решений других кандидатов.
- Если ты используешь open-source модели, библиотеки или готовые подходы, укажи это в описании решения.
- Решение должно воспроизводиться у проверяющего локально, без обращений к внешним API.
- **Очень важно сопровождать решение комментариями.** Голый код без пояснений не приветствуется.

Допускается использование:

- классических ML-моделей;
- нейросетевых моделей;
- методов информационного поиска;
- правил и эвристик;
- гибридных подходов;
- open-source моделей и библиотек, если они разворачиваются локально и не обращаются к внешнему API.

|   |
| - |